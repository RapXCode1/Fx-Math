package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Dark Mode - Clean Human-Designed iOS Midnight Theme
val DeepSpaceBlack = Color(0xFF09090B) // Luxurious absolute dark midnight charcoal
val CardGlassDark = Color(0x22FFFFFF) // Premium high-transparency glass background
val CardGlassLight = Color(0xFFF4F4F5) // Clean premium iOS light grey

// Refined, Classic Professional iOS Apple Accent Colors (No artificial neon/purple glows)
val AccentIndigo = Color(0xFF0A84FF) // Sophisticated iOS Active Blue
val AccentIndigoLight = Color(0xFF409CFF) // iOS Light Blue
val AccentPurple = Color(0xFF2C2C2E) // Deep iOS Dark Grey
val AccentBlue = Color(0xFF8E8E93) // Classic iOS System Gray
val AccentPink = Color(0xFFFF453A) // Beautiful Vivid iOS Coral/Red for destructive states (Delete, Sweep, Warning)

// Map existing color names to maintain full system compatibility
val DarkPrimary = AccentIndigo
val DarkSecondary = Color(0xFF1C1C1E) // iOS secondary grouping
val DarkBackground = DeepSpaceBlack
val DarkSurface = Color(0xFF1C1C1E)
val DarkOnPrimary = Color.White
val DarkOnBackground = Color.White
val DarkOnSurface = Color.White

// Glassmorphic borders
val GlassBorderDark = Color(0x1CFFFFFF) // Crisp, thin light-reflecting glass edge border
val GlassBorderLight = Color(0x1A000000)

// Calculator Button Colors matching iOS Liquid Glass aesthetics
val CalcNumBg = Color(0x19FFFFFF) // Semi-transparent glass for numeric keys (shows background beautifully)
val CalcSpecialBg = Color(0x33FFFFFF) // Distinct medium-light glass for utility keys (C, %, ⌫)
val CalcOpBg = Color(0xFFFF9F0A) // Warm Apple Orange for operational keys (+, -, *, ÷, =)
val CalcFnBg = Color(0x11FFFFFF) // Soft glass sheen for secondary scientific keys

